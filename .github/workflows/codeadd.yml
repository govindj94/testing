name: Add Code to Python File

on:
  push:
    branches:
      - main

jobs:
  Add-code:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Add code to Python file
        run: |
          sed -e '$r database_template.txt' -i ./dev_us.yml

      - name: Commit changes
        uses: EndBug/add-and-commit@v9.1.1
        with:
          github-token: ${{ secrets.PAT }}
          author_name: govindj94
          author_email: govindj94@gmail.com
          message: Append Test

    
